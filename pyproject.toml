[project]
name = "compact-guard"
version = "0.0.1"
description = "Help Claude Code keep its working state through context compaction."
requires-python = ">=3.8"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
